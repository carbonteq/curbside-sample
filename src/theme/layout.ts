// Layout constants — raw px values are permitted here (hook excludes */theme/*)

/** Max-width for the institution search bar, using CSS min() for responsiveness. */
export const SEARCH_BAR_MAX_WIDTH = "min(640px, 100%)";
